class Solution:
    def countSmaller(self, nums: List[int]) -> List[int]:
        ans=[]
        arr=[]
        for i in range(len(nums)-1,-1,-1):
            a=bisect.bisect_left(arr,nums[i])
            ans.append(a)
            insort(arr,nums[i])
        return ans[::-1] 
        

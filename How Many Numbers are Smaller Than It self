class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        num=sorted(nums)
        for i in range(len(nums)):
            nums[i]=num.index(nums[i])
        return nums
        

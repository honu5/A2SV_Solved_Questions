class Solution:
    def countGoodNumbers(self, n: int) -> int:
        MOD=(10**9)+7
        e_pos=(n+1)//2
        o_pos=n//2
        evens=pow(5,e_pos,MOD)
        odds=pow(4,o_pos,MOD)
        return (evens*odds)%MOD

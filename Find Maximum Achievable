class Solution(object):
    def restoreString(self, s, indices):
        """
        :type s: str
        :type indices: List[int]
        :rtype: str
        """
        lst=list(s)
        for i in range(len(lst)):
            lst[indices[i]]=s[i]
        return "".join(lst)

        
